<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dibpm"
        tools:targetApi="31">

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".menu.MenuActivity"/>
        <activity android:name=".DisplacementRegisterActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".FilterActivity"/>
        <activity android:name=".gestion_area_y_empleado.GestionActivity"/>
        <activity android:name=".heritage_asset.HeritageAssetRegisterActivity" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity
            android:name=".gestion_area_y_empleado.areaClass"
            android:exported="true"
            android:theme="@style/Theme.Dibpm">
        </activity>
        <activity
            android:name=".gestion_area_y_empleado.empleadoClass"
            android:exported="true"
            android:theme="@style/Theme.Dibpm">
        </activity>
    </application>
</manifest>
